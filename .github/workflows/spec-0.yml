name: SPEC-0 Check

on:
  push:
    paths:
      - '**/requirements.txt'
      - '**/setup.py'
      - '**/pyproject.toml'
  pull_request:

jobs:
  spec0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: Marcello-Sega/spec0-compliance-bot@v1
        with:
          path: .
